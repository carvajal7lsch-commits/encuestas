version: '3.8'

services:
  db:
    image: postgres:15
    container_name: encuestas_vps_db
    restart: always
    environment:
      POSTGRES_USER: postgres
      POSTGRES_PASSWORD: root
      POSTGRES_DB: modulocrud
    volumes:
      - encuestas_pgdata:/var/lib/postgresql/data
    ports:
      - "5433:5432"

  backend:
    build: ./backend
    container_name: encuestas_vps_backend
    restart: always
    depends_on:
      - db
    environment:
      PORT: 3000
      DB_HOST: db
      DB_USER: postgres
      DB_PASSWORD: root
      DB_NAME: modulocrud
      DB_PORT: 5432
      JWT_SECRET: super_secret_jwt_key
    ports:
      - "3000:3000"

  frontend:
    build:
      context: ./frontend
      args:
        - VITE_API_URL=/api
    container_name: encuestas_vps_frontend
    restart: always
    depends_on:
      - backend
    ports:
      - "8080:80" # Cambiado a 8080 por si el 80 ya está ocupado por otro frontend en Docker

volumes:
  encuestas_pgdata:
